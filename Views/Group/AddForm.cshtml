@model List<Language>

<div class="addingForm">
    <p class="formHeader"> Adding new group </p>
    <hr>
    <form method="post" asp-controller="Group" asp-action="Add">      
        @for(int i = 0; i < Model?.Count; i++)
        {
            <div class="addingSubForm">
                <p class="formHeader"> @(Model[i].Name): </p>
                <input readonly type="number" name="group.GroupNames[@i].Language.Id" value="@Model[i].Id" style="display: none;">
                <input type="text" name="group.GroupNames[@i].Name" placeholder="Enter group's name" class="formInput">
            </div>
        }
        <input type="submit" class="formButton" value="Add group">
    </form>
</div>